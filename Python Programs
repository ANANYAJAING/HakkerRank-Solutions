# 38.Mod-powermode
Solutions:

a=int(input())
b=int(input())
m=int(input())
print(pow(a,b))
print(pow(a,b,m))
--------------------------------------------------------------------------------------------------------------------------------
# power(a,b,m) means a**b mod m
                     a**b 'a'power'b' 

