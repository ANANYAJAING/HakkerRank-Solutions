# 40.Integers come in all sizes
Solution:

import math
a=int(input())
b=int(input())
c=int(input())
d=int(input())
res=((pow(a,b))+(pow(c,d)))
print(res)
--------------------------------------------------------------------------------------------------------------------------------

res=((pow(a,b))+(pow(c,d))) means (a power b)+(c power d) 
