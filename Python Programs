# 34.Check super subset
Solutions:

store=set(input().split())
N=int(input())
output=True
for i in range(N):
    store2=set(input().split())
    if not store2.issubset(store):
        output=False
        if len(store2) >= len(store):
            output=False
print(output)
