# 44.Itertools combinations_with_replacement

Solution:

from itertools import combinations_with_replacement
s,n=input().split()
for j in list(combinations_with_replacement(sorted(s),int(n))):
        print(''.join(j))
   


