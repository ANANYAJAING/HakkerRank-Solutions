# 40.Integers come in all sizes
Solution:

import math
a=int(input())
b=int(input())
c=int(input())
d=int(input())
res=((pow(a,b))+(pow(c,d))) or # we can directly print the result like
print(res)                     # print(((pow(a,b))+(pow(c,d))))
--------------------------------------------------------------------------------------------------------------------------------

res=((pow(a,b))+(pow(c,d))) means (a power b)+(c power d) 
