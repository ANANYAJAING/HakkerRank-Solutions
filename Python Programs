# 31 .symmetric_difference()
Solutions:

n=int(input())
n1=set(map(int,input().split()))
m=int(input())
m1=set(map(int,input().split()))
ans=(n1.symmetric_difference((m1)))
print(len(ans))
-------------------------------------------------------------------------------------------------------------------------------
# .symmetric_difference() means elements which are in n1 and in m1 but not in both
